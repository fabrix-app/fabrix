// import * as assert from 'assert'
import { FabrixApp } from '../../dist/index.js'
import { testApp } from '../fixtures/app.js'

describe('Browser', () => {
  describe('sanity', () => {
    it('should exist', () => {
      const app = new FabrixApp(testApp)
      // assert(app)

    })
  })
})